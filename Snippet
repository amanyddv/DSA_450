#include <bits/stdc++.h>
using namespace std;
 
#define  forn(i,n)  for(int i=0;i<n;i++)
#define  forw(j,n)  for(int j=n-1;j>=0;j--)
#define pb push_back
#define ll long long
int x;
 

 solve(){
   int n;
   cin>>n;
   
   return 
}
 
int main () {
  int T;
  cin >> T;
  while (T--) {
    cout<<solve();
    cout<<endl;
  }
  return 0;
}
